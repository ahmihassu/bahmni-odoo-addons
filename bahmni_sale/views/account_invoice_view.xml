<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<record id="inherit_view_invoice_form_shop" model="ir.ui.view">
		<field name="name">inherit.account.invoice.form.shop</field>
		<field name="model">account.invoice</field>
		<field name="inherit_id" ref="account.invoice_form"/>
		<field name="arch" type="xml">
			<xpath expr="//field[@name='date_invoice']" position="after">
				<field name="shop_id"
					options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
				<field name="payment_method"/>
				<field name="patient_partner_id"
					attrs="{'invisible': [('patient_partner_id', '=', False)]}"/>
				<field name="payer_partner_id"
					attrs="{'invisible': [('payment_method', '!=', 'Credit')]}"/>
				<field name="credit_information"
					attrs="{'invisible': [('payment_method', '!=', 'Credit')]}"/>
				<field name="free_reason"
					attrs="{'invisible': [('payment_method', '!=', 'Free')]}"/>
			</xpath>
			<!-- Odoo 10 customer form: Register Payment is the payment window action. -->
			<xpath expr="//button[@name='%(account.action_account_invoice_payment)d']" position="replace">
				<button name="%(account.action_account_invoice_payment)d" type="action"
					string="Register Payment" groups="account.group_account_invoice"
					class="oe_highlight"
					attrs="{'invisible': ['|', ('state', '!=', 'open'), ('payment_method', '=', 'Credit')]}"/>
			</xpath>
		</field>
	</record>
</odoo>
